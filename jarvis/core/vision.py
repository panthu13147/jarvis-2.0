"""
JARVIS v2.0 — Vision subsystem (reserved).

The active vision path is `jarvis.core.tools.take_screenshot`, which captures the
screen and runs a Vision LLM over it. The earlier `ScreenBuffer` (mss ring-buffer)
prototype was unused (never imported anywhere) and has been removed to avoid dead
code. This module is intentionally kept as a placeholder for future proactive
screen-context features.
"""
